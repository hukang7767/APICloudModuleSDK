<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/dialog_loading_width"
    android:layout_height="@dimen/dialog_loading_height"
    android:layout_gravity="center"
    android:background="@drawable/dialog_background"
    android:orientation="horizontal">

    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_marginLeft="@dimen/dialog_loading_margin"
        android:layout_marginRight="@dimen/dialog_loading_margin"
        android:indeterminateDrawable="@drawable/loading_dialog"
        android:gravity="center_vertical" />

    <TextView
        android:id="@+id/dialog_loading_content"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="center_vertical"
        android:text="@string/loading"
        android:textColor="@color/dialog_title"
        android:textSize="13dp" />

</LinearLayout>
